#include <stdio.h>
int main(){
    int num;
    scanf("%d", &num);
    int arr[num];
    int g = 1;
    for(int i = 0; i < num; i++)
    {
        scanf("%d ",&arr[i]);
    }
    for(int i = 1; i < num; i++)
    {
        if(arr[i-1]>arr[i])
        g++;
    }
    printf("%d",g);
}
